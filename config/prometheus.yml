global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'api_golang'
    static_configs:
      - targets: ['api:8080']
  # Exemplo de monitoramento do rabbitmq e prometheus client se tivessem exporters expostos:
  # - job_name: 'rabbitmq'
  #   static_configs:
  #     - targets: ['rabbitmq:15692']
